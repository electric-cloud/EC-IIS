<step>
    <project>/plugins/@PLUGIN_KEY@/project</project>
    <procedure>RecycleAppPool</procedure>
    <category>Application Server</category>
	<editor>
		<label>Application Server - IIS6 RecycleAppPool</label>
		<version>@PLUGIN_VERSION@</version>
		<description>This procedure recycles an application pool. When recycling occurs, the worker process currently serving the application pool terminates and the WWW service restarts a new worker process to replace it.</description>	
		<formElement>
			<type>entry</type>
			<label>Host name:</label>
			<property>HostName</property>
			<required>1</required>
			<value>localhost</value>
			<documentation>Provide the host name of the IIS server (if the procedure is run on the host itself, use "localhost").</documentation>
		</formElement>		
		<formElement>
			<type>entry</type>
			<label>Application pool name:</label>
			<property>apppoolname</property>
			<required>1</required>
			<documentation>Provide the name of the application pool to recycle.</documentation>
		</formElement>		
	</editor>
</step>
